#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef vector<ll> vl;

#define F first
#define S second
#define PB push_back
#define FOR(n) for (int i = 0; i < n; i++)

void solve()
{
    ll n;
    cin >> n;

    FOR(n)
    cout << i << "\n";
}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    ll loop;
    cin >> loop;
    while (loop--)
        solve();
}
