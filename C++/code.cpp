#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef vector<ll> vl;

#define F first
#define S second
#define PB push_back
#define FOR(i, a, b) for (ll i = a; i <= b; i++)

void solve()
{
    ll n;
    cin >> n;

    FOR(i, 0, n - 1)
    cout << i << "\n";
}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    ll loop;
    cin >> loop;
    while (loop--)
        solve();
}
